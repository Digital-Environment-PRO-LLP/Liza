/// Единый лимит длины названия чата/группы/канала/компании (LABA-2536).
///
/// Это НЕ зеркало серверной константы: Synapse длину `m.room.name` не
/// валидирует вообще (в отличие от displayname — `MAX_DISPLAYNAME_LEN=256`,
/// `servers/synapse/src/synapse/handlers/profile.py:58`). Единственный
/// серверный потолок — размер всего события (`MAX_PDU_SIZE=65536`). Поэтому
/// длинное имя молча сохраняется и потом ломает вёрстку везде, где рендерится.
///
/// Значение выровнено на единственный реально действовавший клиентский лимит —
/// диалог «+» внутри компании (`pages/chat_list/space_view.dart`). В бюджет
/// alias'а публичного канала/пространства (`MAX_ALIAS_LENGTH=255`, алиас
/// выводится из имени в `pages/new_group/new_group.dart`) укладывается с запасом.
///
/// Считается грапемными кластерами (так работает `TextField.maxLength`).
/// Валидатора по `runes` здесь намеренно нет — он оправдан только там, где
/// сервер сам считает кодпойнты и вернёт 400 (случай displayname).
const int maxRoomNameLength = 64;
