<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">
    <!-- io.flutter.app.FlutterApplication is an android.app.Application that
         calls FlutterMain.startInitialization(this); in its onCreate method.
         In most cases you can leave this as-is, but you if you want to provide
         additional functionality it is fine to subclass or reimplement
         FlutterApplication and put your custom class here. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Доступ к телефонной книге — фича «Добавить контакты»: матчинг с Liza
         и приглашение отсутствующих (howItWoks/addContacts.md). -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Android 13+: без декларации runtime-запрос разрешения на уведомления
         не работает — пуши приходят, но ОС молча не показывает банер/звук. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- To make app compatible with tablets -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <uses-sdk
            tools:overrideLibrary="io.wazo.callkeep, net.touchcapture.qr.flutterqr, com.cloudwebrtc.webrtc, org.webrtc, com.it_nomads.fluttersecurestorage, com.pichillilorenzo.flutter_inappwebview, com.example.video_compress, com.otaliastudios.transcoder, com.otaliastudios.opengl, com.kineapps.flutter_file_dialog, com.llfbandit.record, com.pravera.flutter_foreground_task"/>
    <application
        android:label="Liza"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:localeConfig="@xml/locale_config"
    >
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:showOnLockScreen="false"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <!-- ⚠️ НЕ УДАЛЯТЬ и НЕ ставить true. Платформенный deep-linking
                 Flutter (FlutterActivity → NavigationChannel.setInitialRoute /
                 pushRouteInformation → go_router) на Android включён ПО
                 УМОЛЧАНИЮ (FlutterActivityLaunchConfigs.deepLinkEnabled без
                 метаданных возвращает true), тогда как на iOS/macOS выключен
                 (FlutterDeepLinkingEnabled по умолчанию false).

                 Из-за этого один и тот же Intent обрабатывался ДВАЖДЫ: go_router
                 + плагин app_links (chat_list.dart:_processIncomingUris). А
                 go_router матчит ТОЛЬКО uri.path, отбрасывая scheme и host, что
                 для custom-scheme ломает всё: `liza://invite/<code>` даёт
                 path=`/<code>` (host `invite` потерян) → промах → onException →
                 go('/') → плюс гонка с app_links → всеядный fallback
                 openMatrixToUrl → бессигильная строка трактуется SDK как
                 user-id → карточка/поиск пользователя вместо цели.

                 Лендинги auth-proxy редиректят именно на `liza://…`, поэтому
                 на Android ломались практически все инвайт-ссылки.

                 Выключение оставляет ЕДИНСТВЕННЫЙ путь обработки ссылок —
                 ручной через app_links → _processIncomingUris, который
                 разбирает URI по scheme+host корректно, — и выравнивает Android
                 с iOS. См. [[RL-deeplink-single-path]]. -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- App can open on notification action -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- App can open https://matrix.to urls -->
            <intent-filter>
               <action android:name="android.intent.action.VIEW" />
               <category android:name="android.intent.category.DEFAULT" />
               <category android:name="android.intent.category.BROWSABLE" />
               <data
                   android:scheme="https"
                   android:host="matrix.to"/>
            </intent-filter>

            <!-- App can open matrix: uris -->
            <intent-filter>
               <action android:name="android.intent.action.VIEW" />
               <category android:name="android.intent.category.DEFAULT" />
               <category android:name="android.intent.category.BROWSABLE" />
               <data android:scheme="matrix" />
            </intent-filter>

            <!-- App can open liza:// uris -->
            <intent-filter>
               <action android:name="android.intent.action.VIEW" />
               <category android:name="android.intent.category.DEFAULT" />
               <category android:name="android.intent.category.BROWSABLE" />
               <data android:scheme="liza" android:host="chat" />
            </intent-filter>

            <!-- Deep-link для инвайт-ссылок: liza://invite/<code> -->
            <intent-filter>
               <action android:name="android.intent.action.VIEW" />
               <category android:name="android.intent.category.DEFAULT" />
               <category android:name="android.intent.category.BROWSABLE" />
               <data android:scheme="liza" android:host="invite" />
            </intent-filter>

            <!-- ProdamusID registration callback -->
            <intent-filter>
               <action android:name="android.intent.action.VIEW" />
               <category android:name="android.intent.category.DEFAULT" />
               <category android:name="android.intent.category.BROWSABLE" />
               <data android:scheme="liza" android:host="register-callback" />
            </intent-filter>

            <!-- Liza-ссылки: весь хост me.liza.ru (/i/<code>, /s/<code>,
                 /c/<handle> и любые будущие слаги лендинга auth-proxy).
                 pathPrefix НЕ указываем сознательно: на me.liza.ru живут
                 произвольные слаги (_LandingDispatch в auth-proxy), и белый
                 список путей означал бы, что каждый новый тип ссылки требует
                 новой сторовой сборки. Нераспознанный путь клиент отдаёт
                 наружу в браузер (см. _processIncomingUris). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="me.liza.ru" />
            </intent-filter>

            <!-- App can open liza://story/<code> (story deep-link) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="liza" android:host="story" />
            </intent-filter>

            <!-- App can open liza://channel/<handle> (лендинг канала редиректит сюда) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="liza" android:host="channel" />
            </intent-filter>

            <!-- App can receive shared text -->
            <intent-filter>
               <action android:name="android.intent.action.SEND" />
               <category android:name="android.intent.category.DEFAULT" />
               <data android:mimeType="text/*" />
            </intent-filter>

            <!-- App can receive shared any type of files -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <activity
          android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
          android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="liza"/>
            </intent-filter>
        </activity>

        <service android:name=".FcmPushService"
          android:exported="false">
          <intent-filter>
            <action android:name="com.google.firebase.MESSAGING_EVENT"/>
          </intent-filter>
        </service>

        <service android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
             android:foregroundServiceType="camera|microphone|mediaProjection">
        </service>

        <!-- From flutter_local_notifications package for notification actions -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

        <!-- FileProvider для pasteboard.writeImage («Скопировать изображение»):
             плагин ищет authority ${applicationId}.provider и кладёт PNG из
             cacheDir в буфер обмена через content:// URI. Authority свободен
             (share_plus/image_picker/open_app_file используют другие суффиксы). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- To make notifications available in Android Auto -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>

        <!-- Sygnal шлёт гибридный пуш с android.notification без icon: системный
             баннер при выгруженном приложении берёт иконку отсюда. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/notifications_icon" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
